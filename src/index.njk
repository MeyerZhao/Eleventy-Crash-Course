---
layout: layout.njk
---

<h1 class="bg-teal-500" >text</h1>

{% for post in collections.post %}
<h2><a href="{{ post.url | url }}" class="text-sky-500">{{ post.data.title }}</a></h2>
<p>{{ post.content | safe }}</p>
{% endfor %}
