<!DOCTYPE html>
<html lang="en" >
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  {# tailwind #}
  {% if env.ELEVENTY_RUN_MODE == "serve" %}
    <link rel="stylesheet" href="{{ '/assets/css/app.out.css' | url }}">
  {% else %}
    <link rel="stylesheet" href="{{ '/assets/css/app.min.css' | url }}">
  {% endif %}
  <title>Document</title>

</head>
{# <body class="  dark:bg-slate-600 dark:text-white"> #}
<body class="tw-bg-[--background] tw-text-[--foreground]">
  {% include "header.njk" %}
  {# <h1 class="text-4xl">layout - default</h1> #}
  {# <h2 class="text-3xl">{{ title | default("my blog") }}</h2> #}
  <main>
  {# <h1 class="text-4xl font-bold text-amber-500"> {{env.ELEVENTY_RUN_MODE}} </h1> #}
    {# Foo is {{ env.FOO }} and baz is {{ env.BAZ }}. #}

    {{ content | safe }}
  </main>
  {% include "footer.njk" %}

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" ></script>
  <script src="{{ '/assets/js/app.js' | url}}"></script>
</body>
</html>





