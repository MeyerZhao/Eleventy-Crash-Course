module.exports = function () {
	return {
		// environment: process.env.NODE_ENV || "development",
		// environment: process.env.NODE_ENV,
		// environment: process.env.ELEVENTY_RUN_MODE
	};
};


// serve
// build
